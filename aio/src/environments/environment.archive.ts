// This is for archived sites, which are hosted at https://vX.bangular.io, where X is the major Bangular version.
export const environment = {
  gaId: 'UA-8594346-15', // Production id (since it is linked from the main site)
  production: true,
  mode: 'archive'
};
